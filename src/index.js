import React from 'react';
import ReactDOM from "react-dom";
import SliderWrapper from '../src/js/components/container/SliderWrapper';

const wrapper = document.getElementById("app");
ReactDOM.render(<SliderWrapper />, wrapper);